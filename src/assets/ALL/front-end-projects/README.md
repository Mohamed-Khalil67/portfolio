# Front End Projects

Drop any front-end app folder in here. The generator auto-tags everything
inside as the **Front End Projects** category (`frontend`).

See `../landing-pages/README.md` for the per-project layout.
